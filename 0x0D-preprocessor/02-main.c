#include <stdio.h>
/**
 *  main - prints the name of the file of the program
 *	was compiled from, followed by a new line
 *
 * Returns : Always 0.
 */
int main(void)
{
	printf("%s\n", __FILE__);

	return (0);
}
